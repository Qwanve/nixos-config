(defwidget launcher []
  (box
    :space-evenly false
    :class "launcher"
    (button
      :onclick "pkill -x tofi-drun || tofi-drun --width 80% --height 80% | xargs -r swaymsg exec -- &"
      (image)
    )
  )
)
